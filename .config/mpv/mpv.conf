# Prefer Japanese audio, then English.
alang=ja,jpn,en,eng
# Prefer English subtitles when a file has multiple subtitle tracks.
slang=en,eng,en-US,en-GB
# If no preferred language matches, fall back to whatever the file marks as default.
subs-fallback=yes
