#!/usr/bin/env bash

set -euo pipefail

if [[ "$(whoami)" == "dmitry" ]]; then
    yadm encrypt
    yadm commit -am "$(curl -sL commit.takx.xyz)" && \
    yadm push --quiet origin main
    echo -e "\nDone!\n\n"
fi

