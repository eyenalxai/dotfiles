#!/usr/bin/env bash

set -euo pipefail

# Returns a list of all the monospace fonts available on the system.
fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|omarchy' | sort -u
