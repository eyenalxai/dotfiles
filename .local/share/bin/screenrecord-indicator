#!/usr/bin/env bash

# Simple screen recording indicator for waybar
# Shows text when gpu-screen-recorder is running.

if pgrep -f "^gpu-screen-recorder" >/dev/null 2>&1; then
  echo '{"text": "<span alpha=\"60%\">REC</span> ON", "tooltip": "Stop recording"}'
else
  echo '{"text": ""}'
fi
