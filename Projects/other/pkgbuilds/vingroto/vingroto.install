post_install() {
  echo ":: The vingroto daemon runs as a systemd user service."
  echo ":: Enable it for the current user with:"
  echo "     systemctl --user enable --now vingroto.service"
  echo ":: The client talks to it over the local api; start it with:"
  echo "     vingroto"
}
